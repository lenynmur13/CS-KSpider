# AI Study Guide - Remaining Work
# ============================================================================

## PHASE 1 (MVP) STATUS: ✓ COMPLETE
All core functionality is implemented and tested (74 tests passing).

Completed features:
  - File ingestion (PPTX, PDF, TXT, MD, video, audio)
  - Text extraction and transcription (OpenAI Whisper)
  - SQLite database with full schema
  - Smart text chunking
  - AI generation (flashcards, quiz, practice test, audio summary)
  - Export formats (JSON, Anki CSV, Markdown)
  - Full CLI with all commands
  - Comprehensive test suite

## PHASE 1 POLISH (Optional Enhancements)
------------------------------------------------------------------------------

### 1. TTS Audio File Generation
Status: NOT IMPLEMENTED
The spec mentions "Audio summary (TTS)" - currently we generate TTS-friendly
text summaries, but don't actually generate audio files.

Tasks:
  [ ] Add TTS provider integration (OpenAI TTS API or alternative)
  [ ] Add `export --format audio` command
  [ ] Generate MP3/WAV files from audio summaries
  [ ] Add playback speed options

Estimated effort: 0.5 days

### 2. Sample Test Files
Status: SCRIPT CREATED, NOT RUN
Created `tests/create_test_files.py` to generate sample PPTX and PDF files.

Tasks:
  [ ] Run `python tests/create_test_files.py` to generate:
      - tests/test_data/sample.pptx
      - tests/test_data/sample.pdf
  [ ] Add integration tests with real sample files

Estimated effort: 0.5 hours

### 3. Jobs Table for Status Tracking
Status: NOT IMPLEMENTED (marked optional in spec)
Could track long-running operations (transcription, generation).

Tasks:
  [ ] Add Job model to database
  [ ] Add job creation/update in CLI
  [ ] Add `jobs list` and `jobs status` commands

Estimated effort: 0.5 days

### 4. Enhanced Cost Guardrails
Status: BASIC IMPLEMENTATION
Currently limits chunks per generation call.

Tasks:
  [ ] Add token estimation before API calls
  [ ] Add `--dry-run` flag to show estimated cost
  [ ] Add daily/monthly token usage tracking
  [ ] Add configurable spending limits

Estimated effort: 0.5 days

### 5. Batch Generation
Status: NOT IMPLEMENTED
Generate materials from multiple documents at once.

Tasks:
  [ ] Add `generate flashcards --all` to process all documents
  [ ] Add `generate flashcards --docs 1,2,3` for specific docs
  [ ] Add progress tracking for batch operations

Estimated effort: 0.5 days

## PHASE 2: WEB UI
------------------------------------------------------------------------------
Status: NOT STARTED

### Backend (FastAPI)

Tasks:
  [ ] Create `study_guide/api/` package
  [ ] Implement endpoints:
      - POST /api/ingest (upload files)
      - GET /api/documents (list documents)
      - GET /api/documents/:id (get document)
      - DELETE /api/documents/:id (delete document)
      - POST /api/generate (create study materials)
      - GET /api/study-sets (list study sets)
      - GET /api/study-sets/:id (get study set)
      - GET /api/export/:id (download export)
  [ ] Add file upload handling with temp storage
  [ ] Add async task queue for long operations (Celery or background tasks)
  [ ] Add CORS configuration
  [ ] Add API documentation (auto-generated by FastAPI)
  [ ] Add rate limiting

Estimated effort: 2-3 days

### Frontend (Next.js)

Tasks:
  [ ] Initialize Next.js project in `frontend/` directory
  [ ] Set up Tailwind CSS and design system
  [ ] Create pages:
      - / (dashboard with stats)
      - /documents (list and search)
      - /documents/:id (view document and chunks)
      - /upload (drag-and-drop file upload)
      - /generate (select doc and generation type)
      - /study-sets (list generated materials)
      - /study-sets/:id (preview and export)
  [ ] Implement components:
      - FileUploader (drag-and-drop, progress)
      - DocumentCard
      - FlashcardViewer (flip animation)
      - QuizPlayer (interactive quiz taking)
      - TestPlayer (timed practice test)
      - ExportButton (format selector)
  [ ] Add real-time progress updates (WebSocket or polling)
  [ ] Add dark mode support
  [ ] Add responsive design

Estimated effort: 3-5 days

### Deployment

Tasks:
  [ ] Containerize Python API (Dockerfile)
  [ ] Set up docker-compose for local dev
  [ ] Deploy frontend to Vercel
  [ ] Deploy API to Railway/Fly.io/DigitalOcean
  [ ] Set up environment variables
  [ ] Add CI/CD pipeline (GitHub Actions)

Estimated effort: 1-2 days

## PHASE 3: ADVANCED FEATURES
------------------------------------------------------------------------------
Status: NOT STARTED

### YouTube Integration

Tasks:
  [ ] Add yt-dlp for video downloading
  [ ] Extract audio from YouTube videos
  [ ] Handle YouTube URL validation
  [ ] Add `ingest --youtube <url>` CLI command
  [ ] Add YouTube URL input in web UI
  [ ] Handle rate limiting and errors

Estimated effort: 1-2 days

### Spaced Repetition

Tasks:
  [ ] Design SRS algorithm (SM-2 or custom)
  [ ] Add review_history table to database
  [ ] Track card performance (ease, interval, next_review)
  [ ] Add `review` command for CLI
  [ ] Create review UI in web app
  [ ] Add daily review notifications

Estimated effort: 2-3 days

### Analytics Dashboard

Tasks:
  [ ] Track study session data
  [ ] Add analytics tables (sessions, performance)
  [ ] Create charts:
      - Cards reviewed over time
      - Accuracy by difficulty
      - Time spent studying
      - Material coverage
  [ ] Add exportable study reports

Estimated effort: 2-3 days

### Multi-User Authentication

Tasks:
  [ ] Add User model and auth tables
  [ ] Implement JWT authentication
  [ ] Add login/register pages
  [ ] Add user-scoped data access
  [ ] Add sharing capabilities
  [ ] Consider OAuth providers (Google, GitHub)

Estimated effort: 2-3 days

### Automated Ingestion

Tasks:
  [ ] Add folder watching (watchdog)
  [ ] Implement scheduled ingestion (cron/APScheduler)
  [ ] Add Google Drive integration
  [ ] Add Dropbox integration
  [ ] Add webhook triggers

Estimated effort: 2-3 days

## TECHNICAL DEBT / IMPROVEMENTS
------------------------------------------------------------------------------

### Code Quality
  [ ] Add type hints throughout (strict mypy)
  [ ] Add docstrings to all public functions
  [ ] Add logging throughout application
  [ ] Add error codes and structured errors

### Testing
  [ ] Add end-to-end tests with real API calls (optional, costly)
  [ ] Add load testing for API endpoints
  [ ] Add UI integration tests (Playwright/Cypress)
  [ ] Increase test coverage to 90%+

### Documentation
  [ ] Add API documentation (OpenAPI/Swagger)
  [ ] Add user guide with screenshots
  [ ] Add developer guide for contributions
  [ ] Add architecture decision records (ADRs)

### Performance
  [ ] Add caching layer for frequent queries
  [ ] Optimize chunk retrieval for large documents
  [ ] Add database indexes for common queries
  [ ] Consider async database operations

## PRIORITY ORDER
------------------------------------------------------------------------------

Recommended order of implementation:

1. [Quick Win] Run sample file generator, verify tests
2. [Phase 2] FastAPI backend - enables web UI development
3. [Phase 2] Next.js frontend - primary user interface
4. [Phase 2] Deployment - make it accessible
5. [Phase 1] TTS audio generation - completes audio summary feature
6. [Phase 3] YouTube integration - highly requested feature
7. [Phase 3] Spaced repetition - core study feature
8. [Phase 3] Analytics - track learning progress
9. [Phase 3] Multi-user auth - enable sharing/collaboration

## QUICK START FOR NEXT SESSION
------------------------------------------------------------------------------

To continue development:

1. Activate environment:
   python -m venv venv && venv\Scripts\activate  # Windows
   pip install -e ".[dev]"

2. Run tests:
   pytest

3. Start CLI:
   python -m study_guide --help

4. For Phase 2 backend:
   pip install fastapi uvicorn python-multipart
   # Create study_guide/api/main.py

5. For Phase 2 frontend:
   npx create-next-app@latest frontend --typescript --tailwind
   cd frontend && npm run dev

## NOTES
------------------------------------------------------------------------------

- All Phase 1 tests pass (74 tests)
- Windows compatibility verified (UTF-8 encoding, ASCII symbols)
- OpenAI API key required for generation features
- FFmpeg required for video/audio processing
- SQLite database is created in ./data/ directory by default
